const log:any = console.log;

import fs from 'fs';

process.stdin.on('data', (buffer):void =>{
    let prmpt = buffer.toString;
})

interface User {
    name: string,
    lastname: string,
    birthdate: number,
    filename: string
}

